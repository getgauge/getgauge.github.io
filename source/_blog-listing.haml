
- page_articles.each do |article|
  %section.blog-block
    %h2= link_to article.title, article
    %span.author= article.data['author']
    %time= article.date.strftime('%b %e, %Y')
    %p.blog-summary
      = article.data['summary']
    %p.tags
      - article.tags.sort.each do |tag|
        = link_to tag, tag_path(tag)
    / %span.comments-info
    /   %i.fa.fa-comment
    /   3 comments

.pagination-section.clearfix
  - if prev_page
    %span.pagination= link_to "Older", prev_page.url
  - else
    %span.disabled Older

  - if next_page
    %span.pagination= link_to "Newer", next_page.url
  - else
    %span.disabled Newer